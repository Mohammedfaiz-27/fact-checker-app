# Database configuration
# Currently using in-memory storage (no MongoDB required)
# To use MongoDB, uncomment below and install pymongo:
# from pymongo import MongoClient
# MONGO_URI = "mongodb://localhost:27017"
# client = MongoClient(MONGO_URI)
# db = client.factchecker_db
